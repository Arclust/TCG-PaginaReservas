<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= evento.titulo_evento %> - Tea Coffee and Games</title>
    <link rel="icon" href="/logotecito.png">
    <link rel="stylesheet" href="/styles.css">
    <style>
    </style>
</head>
<body>
    <div class="contenedor-color">
        <img src="/logotecito.png" alt="logotecito" class="contenedor-ajustado" width="180px">
        <div class="contenedor-botones">
            <a href="/profile" class="boton-enlace">Perfil</a>
            <a href="/" class="boton-enlace">Calendario</a>
            <a href="/auth/google" class="boton-enlace">Iniciar sesión con Google</a>
        </div>
    </div>
    <div class="contenedor-interfaz">
        <div class="detallesSUB-container">
            <div class="detallesBG-container"></div>
            <div class="detalles-container">
                <div class="event-details">
                    <div class="event-title"><%= evento.titulo_evento %></div>
                    <div class="event-description"><%= evento.descripcion_evento %></div>
                    <div class="event-available-spots">Cupos disponibles: <%= evento.cupos_evento %></div>
                    <div class="event-game">Juego: <%= evento.juego_evento %></div>
                    <div class="event-date">Fecha del evento: <%= evento.fecha_evento.toLocaleString() %></div>
                    <div class="event-price">Precio: $<%= evento.precio_evento %></div>
                    <form action="/inscribir-usuario/<%= evento.ID_evento %>" method="POST">
                        <label for="credencial_inscripcion">¿Cual es tu credencial?</label>
                        <select id="number" name="credencial_inscripcion" required>
                            <% for (let i = 0; i < credenciales.length; i++) { %>
                                <option value="<%= credenciales[i].numero_credencial %>"><%= credenciales[i].juego_credencial %></option>
                            <% } %>
                        </select>
                        <button type="submit">Inscribirse al Evento</button>
                    </form>
                    
                    <!-- Mensaje para usuarios no logueados -->
                    <% if (!user) { %>
                        <p>Por favor, inicia sesión para inscribirte en el evento.</p>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
    <script src="/script.js" type="text/javascript"></script>
</body>
</html>
